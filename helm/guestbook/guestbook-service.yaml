kind: Service
apiVersion: v1
metadata:
  name: guestbook
  labels:
    app: guestbook
spec:
  ports:
  - name: http
    port: 80
    targetPort: http-server
  selector:
    app: guestbook
  type: LoadBalancer